import { fileURLToPath } from "url";
import { dirname, join } from "path";


export const __filename = join(dirname(fileURLToPath(import.meta.url)),"/..")
export const __dirname = dirname(__filename)

