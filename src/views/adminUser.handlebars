<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Administrar Usuario</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Administrar Usuarios</h1>
        {{#each users}}
        <div class="card my-3">
            <div class="card-body">
                <h2 class="card-title">Información del Usuario</h2>
                <p class="card-text"><strong>ID:</strong> {{this._doc._id}}</p>
                <p class="card-text"><strong>Nombre:</strong> {{this._doc.first_name}}</p>
                <p class="card-text"><strong>Rol:</strong> {{this._doc.role}}</p>
            </div>
            <div class="card-footer">
                <h2>Modificar Rol</h2>
                <form method="GET" action="/api/users/{{this._doc._id}}/update-role">
                    <button type="submit" class="btn btn-primary">Cambiar Rol</button>
                </form>
            </div>
            <div class="card-footer">
                <h2>Eliminar Usuario</h2>
                <form method="GET" action="/api/users/{{this._doc._id}}/delete">
                    <button type="submit" class="btn btn-danger">Eliminar Usuario</button>
                </form>
            </div>
        </div>
        {{/each}}
    </div>
    <div class="text-center">
            <a href="/sessions/register" class="btn btn-success">Volver a la pagina de registro</a>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>

